{
  "name": "cameronjgray.com",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Personal CV site - static, fast, minimal.",
  "scripts": {
    "build": "node tools/build-images.js",
    "clean": "node tools/clean.js",
    "rebuild": "npm run clean && npm run build"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  },
  "engines": {
    "node": ">=20"
  }
}
