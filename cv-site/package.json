{
  "name": "cameronjgray.com",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Personal CV site - static, fast, minimal.",
  "scripts": {
    "images": "node tools/build-images.js",
    "compress": "node tools/compress.js",
    "build": "npm run images && npm run compress",
    "clean": "node tools/clean.js",
    "rebuild": "npm run clean && npm run build"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  },
  "engines": {
    "node": ">=20"
  }
}
